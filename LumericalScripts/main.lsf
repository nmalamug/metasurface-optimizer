#Initialize the functions from the library
constructive_sweep_funct;
analytical_sweep_funct;

#Initialize parameters
xLength = linspace(150,550,17);
yLength = linspace(150,550,5);

#Do constructive and analytical sweeps 
#To do analytical only, comment out the constructive sweep

constructivesweep(xLength, yLength);
data = analyticalsweep(xLength, yLength);
visualizedata(data);